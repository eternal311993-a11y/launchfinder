{
  "name": "launchfinder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/launch-pack.js",
    "postinstall": "npm run check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "openai": "^5.20.0",
    "stripe": "^18.5.0"
  }
}
