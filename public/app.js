const form=document.querySelector("#form"),idea=document.querySelector("#idea"),results=document.querySelector("#results"),cards=document.querySelector("#cards"),btn=form.querySelector("button");
form.addEventListener("submit",async e=>{e.preventDefault();btn.disabled=true;btn.textContent="Creating ideas…";cards.innerHTML="";try{const r=await fetch("/api/generate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({idea:idea.value})});const d=await r.json();if(!r.ok)throw new Error(d.error);for(const x of d.names){const el=document.createElement("article");el.className="card";el.innerHTML=`<h3>${escapeHtml(x.name)}</h3><div class="domain">${escapeHtml(x.domain)}</div><a class="buy" target="_blank" rel="noopener sponsored" href="https://www.name.com/domain/search/${encodeURIComponent(x.domain)}">Check domain →</a>`;cards.appendChild(el)}results.classList.remove("hidden");results.scrollIntoView({behavior:"smooth"});}catch(err){alert(err.message||"Something went wrong.")}finally{btn.disabled=false;btn.textContent="Find my brand →"}});
function escapeHtml(s){const d=document.createElement("div");d.textContent=s;return d.innerHTML}